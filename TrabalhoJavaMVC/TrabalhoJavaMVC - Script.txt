create database trabalhojavamvc

use trabalhojavamvc

CREATE TABLE Carro (
id int NOT NULL AUTO_INCREMENT PRIMARY KEY,
marca varchar(50),
modelo varchar(50),
cor varchar(50),
anomodelo Int(10)
);
